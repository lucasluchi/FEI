# Faça um programa que leia o valor de um raio inserido pelo usuário e cálcule a área do círculo

raio = float(input("Entre com o valor do raio em metros (ex: 2.5): "))
pi = 3.14159

print("Área do círculo é de %.2f m2" % (pi * raio**2))